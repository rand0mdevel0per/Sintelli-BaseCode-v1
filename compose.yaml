services:
  python-40_cutlass_py:
    build:
      context: ./cutlass/examples/40_cutlass_py
    container_name: python-40_cutlass_py
    restart: unless-stopped
    # env_file: ./.env  # Uncomment if .env file exists in ./cutlass/examples/40_cutlass_py
    # No ports exposed; adjust if README or code specifies any
    # No external dependencies detected

  python-41_fused_multi_head_attention:
    build:
      context: ./cutlass/examples/41_fused_multi_head_attention
    container_name: python-41_fused_multi_head_attention
    restart: unless-stopped
    # env_file: ./.env  # Uncomment if .env file exists in ./cutlass/examples/41_fused_multi_head_attention
    # No ports exposed; adjust if README or code specifies any
    # No external dependencies detected
    # Default command runs fmha_backward_test.py (see Dockerfile)

  python-ampere:
    build:
      context: ./cutlass/python/CuTeDSL/ampere
    container_name: python-ampere
    restart: unless-stopped
    # env_file: ./.env  # Uncomment if .env file exists in ./cutlass/python/CuTeDSL/ampere
    # No ports exposed; adjust if README or code specifies any
    # No external dependencies detected

  python-blackwell:
    build:
      context: ./cutlass/python/CuTeDSL/blackwell
    container_name: python-blackwell
    restart: unless-stopped
    # env_file: ./.env  # Uncomment if .env file exists in ./cutlass/python/CuTeDSL/blackwell
    # No ports exposed; adjust if README or code specifies any
    # No external dependencies detected

  python-cute-ffi:
    build:
      context: ./cutlass/python/CuTeDSL/cute/ffi
    container_name: python-cute-ffi
    restart: unless-stopped
    # env_file: ./.env  # Uncomment if .env file exists in ./cutlass/python/CuTeDSL/cute/ffi
    # No ports exposed; adjust if README or code specifies any
    # No external dependencies detected

  python-hopper:
    build:
      context: ./cutlass/python/CuTeDSL/hopper
    container_name: python-hopper
    restart: unless-stopped
    # env_file: ./.env  # Uncomment if .env file exists in ./cutlass/python/CuTeDSL/hopper
    # No ports exposed; adjust if README or code specifies any
    # No external dependencies detected

# No external services (databases, caches, etc.) detected in documentation or code
# No networks section needed as no inter-service communication is required
